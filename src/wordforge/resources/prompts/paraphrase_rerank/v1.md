Reorder the given meanings by *modern usage frequency*, most common first.

Input word: {word}

Current meanings (as a JSON array; each has "cn" and optionally "en"/"pos"):
{meanings_json}

Task:
- Return the INDICES of the input meanings in frequency order (index 0 = most common in modern everyday usage, colloquial speech, typical learner encounters).
- Include every index exactly once. Do NOT add or remove meanings.
- Base "frequency" on how often a B1-B2 learner would encounter this sense. Collins-style dictionary order is often history-biased — prefer modern spoken/written usage.
- Specifically demote archaic, poetic, technical-jargon, or niche register senses. Promote senses that appear in daily conversation, news, textbooks.

Output schema (strict JSON, no markdown fence, no prose):
{
  "order": [<int>, <int>, ...]
}

Example (conceptual — input meanings listed CN only):
  input meanings: ["如此 (定语从句辅助)", "因此", "为了", "如此 (形容词强调)", "也一样"]
  good output: {"order": [1, 2, 3, 0, 4]}     # 因此/为了/强调如此 最常用

Return ONLY the JSON.
